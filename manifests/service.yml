apiVersion: v1
kind: Service
metadata:
    name: "eefefef-c0ca"
    labels:
        app: "eefefef-c0ca"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "eefefef-c0ca"